<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:titleTextColor="@android:color/white"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <EditText
                android:id="@+id/etStartPoint"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginBottom="16dp"
                android:background="@drawable/edit_text_bg"
                android:padding="12dp"
                android:hint="Start Point (e.g. Downtown)"
                android:inputType="textPostalAddress" />

            <EditText
                android:id="@+id/etDestination"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginBottom="16dp"
                android:background="@drawable/edit_text_bg"
                android:padding="12dp"
                android:hint="Destination (e.g. Airport)"
                android:inputType="textPostalAddress" />

            <EditText
                android:id="@+id/etWaypoints"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginBottom="16dp"
                android:background="@drawable/edit_text_bg"
                android:padding="12dp"
                android:hint="Waypoints (Optional)"
                android:inputType="text" />

            <EditText
                android:id="@+id/etTimeWindow"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginBottom="16dp"
                android:background="@drawable/edit_text_bg"
                android:padding="12dp"
                android:hint="Departure Time Window (e.g. 07:30-08:00)"
                android:inputType="text" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:weightSum="2"
                android:layout_marginBottom="16dp">

                <EditText
                    android:id="@+id/etSeats"
                    android:layout_width="0dp"
                    android:layout_height="56dp"
                    android:layout_weight="1"
                    android:layout_marginEnd="8dp"
                    android:background="@drawable/edit_text_bg"
                    android:padding="12dp"
                    android:hint="Total Seats"
                    android:inputType="number" />

                <EditText
                    android:id="@+id/etFare"
                    android:layout_width="0dp"
                    android:layout_height="56dp"
                    android:layout_weight="1"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/edit_text_bg"
                    android:padding="12dp"
                    android:hint="Fare per Seat"
                    android:inputType="numberDecimal" />
            </LinearLayout>

            <EditText
                android:id="@+id/etDistance"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginBottom="16dp"
                android:background="@drawable/edit_text_bg"
                android:padding="12dp"
                android:hint="Estimated Distance (km)"
                android:inputType="numberDecimal" />


            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switchActive"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="24dp"
                android:text="Route is Active"
                android:textSize="16sp"
                app:showText="false"
                android:checked="true" />

            <Button
                android:id="@+id/btnSaveRoute"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="Save Route"
                android:textSize="18sp"
                android:background="@drawable/button_primary"
                android:textColor="@android:color/white" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
